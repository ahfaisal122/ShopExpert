@model IEnumerable<ShopEx.Models.advertisement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.information)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.validity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.information)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.validity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.advertisementId }) |
            @Html.ActionLink("Details", "Details", new { id=item.advertisementId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.advertisementId })
        </td>
    </tr>
}

</table>
